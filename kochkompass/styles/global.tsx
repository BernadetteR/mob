import {StyleSheet} from "react-native";

export const globalStyles = StyleSheet.create({
    globalContainer: {
        backgroundColor: 'white',
    },
    globalHeadline: {
        fontSize: 25,
        textAlign: 'center',
        marginTop: 10,
        marginBottom: 10,
    }

})